## Initialize: swiper-gen

**Status:** ✅ Created at `code/swiper-gen/`

**Dependencies:**
- @biomejs/biome (v2.3.4)
- typescript (v5.9.3)
- @types/bun (latest)

**Configuration:**
- `package.json` scripts: lint, test, type-check
- `biome.json` with strict linting rules
- `tsconfig.json` with strict mode enabled
- Directory structure: `src/` and `tests/`

**Next:** validate-terminal-capability
